/** Target resale markup applied when deriving plan target price from asking vs estimate. */
export const TARGET_MARKUP = 1.08;

/** Draw count for the seeded triangular deal robustness stress test. */
export const ROBUSTNESS_DRAWS = 5_000;

/** Confidence ratio at or above which wide-band risk flags are emitted. */
export const WIDE_CONFIDENCE_RATIO = 0.16;
