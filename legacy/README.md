# Legacy Notes

I moved older project layers here so the active app is easier to understand.

- `dash-prototype/`: the first Dash version of the idea.
- `frontend-three-step-pages/`: the earlier Estimate -> Improve -> Plan React flow.
- `dormant-db/`: an unused Postgres/Drizzle prototype and old API data constants.
- `model-artifacts/`: older local model bundles. This folder is ignored by git.

The active product now lives in:

- `artifacts/home-value-planner`
- `artifacts/api-server`
- `artifacts/model-service`
- `artifacts/shared`
